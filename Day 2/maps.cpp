// @author: Laksh Sadhwani
// Code : (What is your code)

#include<bits/stdc++.h>
using namespace std;
//#define S 5
// struct S{
// 	int x;
// };
typedef pair<int, int> iPair;
typedef vector<int> V;
typedef vector<bool> B;
typedef tuple<int,int,int> T;
int main()
{
// int a[10] = { 2, 4, 5, 6, 8, 9, 4, 2, 6, 5 };
// priority_queue<int> pq;
// for(auto i : a)
// 	pq.push(i);
// while(!pq.empty()){
// 	cout << pq.top() << " ";
// 	pq.pop();
// 	// priority queue don't have iterators
map<int,int> mpp;
mpp[Something]= value;
cout<<mpp[Something];
// value
arr = {1,2,3,4,4,5};
map<int,int> mpp;
for(auto it : arr){
	mpp[it]++;
}
{1-1
 2-1
 3-1
 4-2
 5-1 }
 if(mpp.find(6)!=mpp.end())
 	cout << "element is present";
 else
 	cout << "not present";

 mpp.insert(make_pair(key,value));
 // erase(arr.begin(), arr.end());
 // erase(arr.begin());
 // erase(5);
 // clear();
 9 ka lowerbound
 1,2,3,3,4,5,5,6,7 end()
}
)O(log n)
return 0;
}